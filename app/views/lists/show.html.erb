<h2><%= @list.name %></h2>
<ul>
	<% @list.items.each do |item| %>
		<li><%= link_to item.title, list_item_path(@list, item) %></li> 
	<% end %>
</ul>
<br />
<br />
<p>
	<%= link_to "Edit list", edit_list_path(@list) %> | 
	<%= link_to "Add an item", new_list_item_path(@list) %> 
</p>